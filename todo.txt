To Do:

 - add reboot + wait after package changes
 - configure repos for nodes to use rubix as a repo source
   - avoid installing internet surprise updates
   - don't forget to adjust kickstart file
 - re-mount ISO handler if download received a new file
 - preserve ssh hostkeys for nodes and rubix

 - if we install postfix, we may need to change its /etc/postfix/main.cf to say
  inet_interfaces = 127.0.0.1
  instead of
  #inet_interfaces = localhost
  to allow it to start.

 - always reboot after configuring network on rubix

 - rebootbenachrichtigung nach updates

 - disk setup plan:
    1. crypt
    2. lvm
    3. Ceph OSD
    4. CephFS

https://ceph.com/install/
https://public.centerdevice.de/d72fea90-0d1f-4f7d-8cf2-404603a94032 -- pdf article
https://www.howtoforge.com/tutorial/how-to-build-a-ceph-cluster-on-centos-7/
http://docs.ceph.com/docs/master/install/
http://docs.ceph.com/docs/master/install/install-storage-cluster/

https://wiki.centos.org/HowTos/CreateLocalMirror
 - see the last section on carrying only a subset of the repo (DVD + updates?)
 - also, http://docs.ansible.com/ansible/latest/modules/yum_repository_module.html

Current plan:
3. NTP auf rubix und kubes
4. Rubix cron-job that runs ansible on itself -> auto update + update centos mirror
5. Kubes auto-updates (? but what if it breaks them ? cron timer that only updates if other responds?)
